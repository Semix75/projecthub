{% extends 'base.html.twig' %}

{% block title %}Modifier mes Vœux{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center">Modifier mes vœux</h1>
    <p class="text-center">
        <a href="{{ path('app_profil') }}">Retour à mon profil</a>
    </p>

    {{ form_start(form) }}

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-body">
                    <p class="text-muted">Sélectionnez vos projets préférés dans l'ordre de préférence.</p>

                    {% for i in 1..5 %}
                        <div class="mb-3">
                            {{ form_label(attribute(form, 'projet_' ~ i)) }}
                            {{ form_widget(attribute(form, 'projet_' ~ i), {'attr': {'class': 'form-select choix-projet'}}) }}
                            {{ form_errors(attribute(form, 'projet_' ~ i)) }}
                        </div>
                    {% endfor %}

                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary">Mettre à jour mes vœux</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {{ form_end(form) }}
</div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const selects = document.querySelectorAll(".choix-projet");

        selects.forEach(select => {
            select.addEventListener("change", function () {
                let selectedValues = new Set();

                // Récupérer tous les projets sélectionnés
                selects.forEach(s => {
                    if (s.value) {
                        selectedValues.add(s.value);
                    }
                });

                // Désactiver les options déjà sélectionnées dans d'autres sélecteurs
                selects.forEach(s => {
                    [...s.options].forEach(option => {
                        if (option.value && selectedValues.has(option.value) && s.value !== option.value) {
                            option.disabled = true;
                        } else {
                            option.disabled = false;
                        }
                    });
                });
            });
        });
    });
</script>

{% endblock %}
