image: php:8.2

services:
  - mysql:5.7

variables:
  MYSQL_DATABASE: projecthub_test
  MYSQL_ROOT_PASSWORD: root
  DATABASE_URL: "mysql://root:root@mysql:3306/projecthub_test"
  NODE_ENV: development

stages:
  - install
  - test
  - build
  - deploy

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - vendor/
    - node_modules/

before_script:
  # Mise à jour et installation des dépendances système (incluant gnupg pour apt-key)
  - apt-get update && apt-get install -y unzip git libicu-dev libxml2-dev libzip-dev gnupg curl
  # Installation des extensions PHP nécessaires
  - docker-php-ext-install pdo pdo_mysql intl zip
  # Installation de Composer
  - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
  # Installation de Node.js et npm
  - apt-get install -y nodejs npm
  # Installation de Yarn : ajout du dépôt officiel et installation
  - curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | apt-key add -
  - echo "deb https://dl.yarnpkg.com/debian/ stable main" | tee /etc/apt/sources.list.d/yarn.list
  - apt-get update && apt-get install -y yarn

install_dependencies:
  stage: install
  script:
    # Installation des dépendances PHP
    - composer install --prefer-dist --no-interaction --optimize-autoloader
    # Installation des dépendances Node.js
    - npm install
    - yarn install

test:
  stage: test
  script:
    # Met à jour le schéma de la base de test
    - php bin/console doctrine:schema:update --force --env=test
    # Exécute les tests PHPUnit
    - vendor/bin/phpunit

build_front:
  stage: build
  script:
    # Correction automatique d'une éventuelle faute de frappe dans vite.config.js
    - sed -i 's/jsxFrament/jsxFragment/g' vite.config.js || true
    # Compilation des assets front-end (vider le dossier de sortie)
    - yarn build --emptyOutDir
  artifacts:
    paths:
      - public/build

deploy:
  stage: deploy
  image: docker:latest
  services:
    - docker:dind
  variables:
    DOCKER_DRIVER: overlay2
    IMAGE_TAG: "$CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG"
  script:
    - echo "Building Docker image..."
    - docker build -t $IMAGE_TAG .
    - echo "Logging into Docker registry..."
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
    - echo "Pushing Docker image..."
    - docker push $IMAGE_TAG
    - echo "Image pushed. Déploiement manuel requis."
  when: manual
  environment:
    name: production
